global:
  namespace: "prod-ns"
  imageTag: "" # Populated by CI
  keyvaultName: "keyvaorg-prod"
  azureTenantId: ""
  userAssignedIdentityID: ""

# High Availability and Full Performance for Production
auth:
  replicaCount: 2
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 512Mi }

ai:
  replicaCount: 2
  resources:
    requests: { cpu: 300m, memory: 512Mi }
    limits: { cpu: 1000m, memory: 1024Mi }

hr:
  replicaCount: 2
  resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 512Mi } }

finance:
  replicaCount: 2
  resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 512Mi } }

projects:
  replicaCount: 2
  resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 512Mi } }

gateway:
  replicaCount: 2
  resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 512Mi } }

frontend:
  replicaCount: 2
  resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 512Mi } }

notification:
  replicaCount: 2
  resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 512Mi } }

ingress:
  sslRedirect: "true"
