name: hr
replicaCount: 2
rollingUpdate:
  type: RollingUpdate
  maxUnavailable: 25%
  maxSurge: 25%

image:
  name: maheshnandi/organistation-hr
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 8002
  targetPort: 8002
  healthPath: /health

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

secrets:
  - name: "MONGODB-URI"
    key: "MONGODB_URI"
  - name: "INTERNAL-SERVICE-SECRET"
    key: "INTERNAL_SERVICE_SECRET"
